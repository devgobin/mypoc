import { CoreDirectivesDirective } from './core-directives.directive';

describe('CoreDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
