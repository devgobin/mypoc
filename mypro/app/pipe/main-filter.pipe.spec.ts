import { MainFilterPipe } from './main-filter.pipe';

describe('MainFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MainFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
