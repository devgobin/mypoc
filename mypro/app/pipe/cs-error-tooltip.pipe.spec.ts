import { CsErrorTooltipPipe } from './cs-error-tooltip.pipe';

describe('CsErrorTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new CsErrorTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
