import { MessagePortalPipe } from './message-portal.pipe';

describe('MessagePortalPipe', () => {
  it('create an instance', () => {
    const pipe = new MessagePortalPipe();
    expect(pipe).toBeTruthy();
  });
});
